---
services:
  bot:
    container_name: esgi-bot
    restart: unless-stopped
    image: esgi-discord-bot:${BOT_VERSION}
    build:
      context: .
    env_file: .env
    volumes:
      - ./volumes/imports:/tmp/imports
      - ./volumes/db:/usr/src/db
...
